import { SectionIconsEnum, Section } from './types';

export const config: { sections: Section[] } = {
    sections: [
        { title: 'Sessions', icon: SectionIconsEnum.app },
        { title: 'Products', icon: SectionIconsEnum.view },
    ],
};
